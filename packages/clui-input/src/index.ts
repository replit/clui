import { createInput, IConfig, IInputUpdates } from './input';

export {
  ICommands,
  ICommand,
  ICommandArgs,
  IArg,
  ArgType,
  ArgTypeDef,
  IOption,
  IRunOptions,
  SubCommands,
} from './types';

export { IConfig, IInputUpdates };

export default createInput;
